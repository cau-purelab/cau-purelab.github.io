# 줄바꿈 정규화 — 저장소에는 LF로만 저장한다.
# (Windows 작업본과 WSL 작업본을 오갈 때 전 파일이 CRLF 변경으로 뜨는 것을 막는다)
* text=auto eol=lf

# 바이너리 자산은 어떤 변환도 하지 않는다.
*.png -text
*.jpg -text
*.jpeg -text
*.webp -text
*.ico -text
*.gif -text
*.pdf -text
*.woff -text
*.woff2 -text
*.ttf -text
*.otf -text
